# User-dashboard
User dashboard web application using angular and spring boot

To run server
```sh
cd /path-to-server/
./mvnw spring-boot:run
```

To run client
```sh
cd /path-to-client/
ng serve -o
$ cd /path-to-client/
$ ng serve -o
```

